upstream frontend{
    server frontend:5555;
}

upstream backend{
    server backend:3030;
}

server {
    listen 80;
    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://backend/;
        
    }
    location / {
        proxy_pass http://frontend;
    }

    
}

